# Ядро (детерминированная часть: трекер, модель цели, видео) — без нейросетей
numpy
opencv-python-headless    # или opencv-python, если нужен --select (окно с кликом)
scipy                     # венгерский алгоритм; без него — жадная ассоциация
imageio-ffmpeg            # статический ffmpeg для HEVC/HDR-роликов iPhone

# Детектор (обязателен для реального видео)
ultralytics               # YOLOv8/11; тянет torch. На Mac: pip install torch torchvision — MPS

# Веб-приложение (python serve.py)
fastapi
uvicorn
python-multipart
httpx                     # ИИ-разбор (DeepSeek API); ключ — DEEPSEEK_API_KEY или data/secrets.json

# Необязательно
# torchreid               # нейросетевой ReID (--encoder osnet): точнее цветовых гистограмм
# easyocr                 # чтение номеров (--ocr easyocr)

# Тесты
pytest
