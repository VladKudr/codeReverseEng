# Валидация требований против кода — subj

| Вердикт | Требований |
|---|---|
| ПОДТВЕРЖДЕНО | 6 |
| ЧАСТИЧНО | 1 |
| ОПРОВЕРГНУТО | 0 |
| НЕ ОТНОСИТСЯ | 0 |

Подтверждено: 6/7 = 85.7%   (порог 97%: НЕ достигнут)

**subj-R-103.** Проверялось чтением константы `DB_URL`: подключение к БД в
этой среде не воспроизводимо (нет сети до db.internal). Расхождение относится
к способу проверки, а не к формулировке: правило возвращается на этап 4 для
переформулировки в наблюдаемое поведение.
